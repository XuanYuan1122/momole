package com.moemoe.lalala.greendao.gen;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.moemoe.lalala.model.entity.ChatUserEntity;

import com.moemoe.lalala.model.entity.ChatContentDbEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CHAT_CONTENT_DB_ENTITY".
*/
public class ChatContentDbEntityDao extends AbstractDao<ChatContentDbEntity, String> {

    public static final String TABLENAME = "CHAT_CONTENT_DB_ENTITY";

    /**
     * Properties of entity ChatContentDbEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Content = new Property(0, String.class, "content", false, "CONTENT");
        public final static Property ContentType = new Property(1, String.class, "contentType", false, "CONTENT_TYPE");
        public final static Property CreateTime = new Property(2, java.util.Date.class, "createTime", false, "CREATE_TIME");
        public final static Property Id = new Property(3, String.class, "id", true, "ID");
        public final static Property TalkId = new Property(4, String.class, "talkId", false, "TALK_ID");
        public final static Property UserId = new Property(5, String.class, "userId", false, "USER_ID");
        public final static Property State = new Property(6, boolean.class, "state", false, "STATE");
    }

    private DaoSession daoSession;


    public ChatContentDbEntityDao(DaoConfig config) {
        super(config);
    }
    
    public ChatContentDbEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CHAT_CONTENT_DB_ENTITY\" (" + //
                "\"CONTENT\" TEXT," + // 0: content
                "\"CONTENT_TYPE\" TEXT," + // 1: contentType
                "\"CREATE_TIME\" INTEGER," + // 2: createTime
                "\"ID\" TEXT PRIMARY KEY NOT NULL ," + // 3: id
                "\"TALK_ID\" TEXT," + // 4: talkId
                "\"USER_ID\" TEXT," + // 5: userId
                "\"STATE\" INTEGER NOT NULL );"); // 6: state
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CHAT_CONTENT_DB_ENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ChatContentDbEntity entity) {
        stmt.clearBindings();
 
        String content = entity.getContent();
        if (content != null) {
            stmt.bindString(1, content);
        }
 
        String contentType = entity.getContentType();
        if (contentType != null) {
            stmt.bindString(2, contentType);
        }
 
        java.util.Date createTime = entity.getCreateTime();
        if (createTime != null) {
            stmt.bindLong(3, createTime.getTime());
        }
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(4, id);
        }
 
        String talkId = entity.getTalkId();
        if (talkId != null) {
            stmt.bindString(5, talkId);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(6, userId);
        }
        stmt.bindLong(7, entity.getState() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ChatContentDbEntity entity) {
        stmt.clearBindings();
 
        String content = entity.getContent();
        if (content != null) {
            stmt.bindString(1, content);
        }
 
        String contentType = entity.getContentType();
        if (contentType != null) {
            stmt.bindString(2, contentType);
        }
 
        java.util.Date createTime = entity.getCreateTime();
        if (createTime != null) {
            stmt.bindLong(3, createTime.getTime());
        }
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(4, id);
        }
 
        String talkId = entity.getTalkId();
        if (talkId != null) {
            stmt.bindString(5, talkId);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(6, userId);
        }
        stmt.bindLong(7, entity.getState() ? 1L: 0L);
    }

    @Override
    protected final void attachEntity(ChatContentDbEntity entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3);
    }    

    @Override
    public ChatContentDbEntity readEntity(Cursor cursor, int offset) {
        ChatContentDbEntity entity = new ChatContentDbEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // content
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // contentType
            cursor.isNull(offset + 2) ? null : new java.util.Date(cursor.getLong(offset + 2)), // createTime
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // id
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // talkId
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // userId
            cursor.getShort(offset + 6) != 0 // state
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ChatContentDbEntity entity, int offset) {
        entity.setContent(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setContentType(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setCreateTime(cursor.isNull(offset + 2) ? null : new java.util.Date(cursor.getLong(offset + 2)));
        entity.setId(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setTalkId(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setUserId(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setState(cursor.getShort(offset + 6) != 0);
     }
    
    @Override
    protected final String updateKeyAfterInsert(ChatContentDbEntity entity, long rowId) {
        return entity.getId();
    }
    
    @Override
    public String getKey(ChatContentDbEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ChatContentDbEntity entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getChatUserEntityDao().getAllColumns());
            builder.append(" FROM CHAT_CONTENT_DB_ENTITY T");
            builder.append(" LEFT JOIN CHAT_USER_ENTITY T0 ON T.\"USER_ID\"=T0.\"USER_ID\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ChatContentDbEntity loadCurrentDeep(Cursor cursor, boolean lock) {
        ChatContentDbEntity entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        ChatUserEntity user = loadCurrentOther(daoSession.getChatUserEntityDao(), cursor, offset);
        entity.setUser(user);

        return entity;    
    }

    public ChatContentDbEntity loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ChatContentDbEntity> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ChatContentDbEntity> list = new ArrayList<ChatContentDbEntity>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ChatContentDbEntity> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ChatContentDbEntity> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
